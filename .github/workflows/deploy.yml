name: Publish to GitHub Packages

on:
  push:
    tags: [ "*" ]
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

permissions:
  contents: write
  packages: write

env:
  GITHUB_OWNER: ${{ github.repository_owner }}
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  GITHUB_REPOSITORY: ${{ github.repository }}
  GITHUB_PACKAGE_USERNAME: ${{ github.repository_owner }}
  GITHUB_PACKAGE_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 23
        uses: actions/setup-java@v4
        with:
          java-version: '23'
          distribution: 'oracle'

      - name: Make Gradle Wrapper Executable
        run: chmod +x gradlew

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: wrapper

      - name: Cache Gradle Dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-
  
      - name: Build Common
        run: |
          ./gradlew :common:build -DincludeModules=common --build-cache --parallel

      - name: Build Paper
        run: |
          ./gradlew :paper:build -DincludeModules=paper --build-cache --parallel

      - name: Build Velocity
        run: |
          ./gradlew :velocity:build -DincludeModules=velocity --build-cache --parallel

      - name: Upload Single Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: all-artifacts
          path: |
            common/build/libs/*.jar
            paper/build/libs/*.jar
            velocity/build/libs/*.jar

      - name: Build Fabric 1.20.1
        run: |
          ./gradlew :fabric:build -Pvariable=1.20.1 -DincludeModules=fabric --build-cache --parallel

      - name: Upload Fabric 1.20.1 Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: all-artifacts
          path: |
            fabric/build/libs/*.jar

      - name: Build Fabric 1.21.4
        run: |
          ./gradlew :fabric:build -Pvariable=1.21.4 -DincludeModules=fabric --build-cache --parallel

      - name: Upload Fabric 1.21.4 Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: all-artifacts
          path: |
            fabric/build/libs/*.jar

  publish:
    needs: build
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - module: common
            variable: global
          - module: paper
            variable: global
          - module: velocity
            variable: global
          - module: fabric
            variable: 1.20.1
          - module: fabric
            variable: 1.21.4
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 23
        uses: actions/setup-java@v4
        with:
          java-version: '23'
          distribution: 'oracle'

      - name: Make Gradle Wrapper Executable
        run: chmod +x gradlew

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: wrapper

      - name: Cache Gradle Dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Publish ${{ matrix.module }}
        run: |
          ./gradlew :${{ matrix.module }}:publish -Pvariable=${{ matrix.variable }} -DincludeModules=${{ matrix.module }} --build-cache --parallel

  release:
    needs: [build, publish]
    runs-on: ubuntu-latest
    if: github.ref_type == 'tag'
    steps:
      - name: Download All Artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: '*-artifacts'
          path: artifacts

      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/**/*.jar