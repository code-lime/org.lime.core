{
  "schemaVersion": 1,
  "id": "${modId}",
  "version": "${version}",
  "name": "${group}.core",
  "description": "",
  "authors": ["Lime"],
  "contact": {},
  "license": "unlicense",
  "environment": "server",
  "entrypoints": {
    "server": [
      "org.lime.core.fabric.CoreFabricMod"
    ]
  },
  "mixins": [
    "org-lime-core-fabric.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${versionLoader}",
    "fabric": "*",
    "minecraft": "${versionMinecraft}"
  }
}
